# report: メンバー動画が既存銘柄の詳細画面に反映されない問題の原因特定(2026-07-07)

## 結論
**実装バグは見つからなかった。** 根本原因は「メンバー動画登録が翌営業日18:30の日次バッチでのみ反映される」設計上のタイムラグであり、修正は行っていない(port 8081系コード・本番dataとも無変更)。

## 調査対象・手順
トレース例: 6769ザインエレクトロニクス(videoId `2pfLv-kCKFI`、addedAt 2026-07-02T16:37:09Z、memberLikely true、codes ["6769"])

1. `youtube_member.json`内の該当動画の存在・内容を確認 → 正しく保存されている
2. 6769はmaster.jsonに既存登録済み。詳細画面のデータソースを特定: `dashboard/data/{code}.json`(build_data.pyが生成)→`GET /api/stocks/{code}`(app.py、`manual_links.json`のみ追記でフィルタなし)→`buildYoutubePanel(s)`(index.html、`s.youtube||[]`をそのまま描画・フィルタなし)
3. `data/6769.json`(2026-07-06 18:30ビルド分)に該当動画が`memberOnly:true`で**正しく反映済み**であることを確認。API層・フロント層とも該当動画を隠すフィルタは存在しない
4. `youtube_member.json`全46件を機械的に照合(各動画のcodeに対応する`data/{code}.json`に該当videoIdが入っているか):
   - **45件は正しく反映済み**
   - 唯一未反映だったのは1980(videoId `cgpC2JrXEYg`)。ただしaddedAtが2026-07-06T23:03(JST)で、同日18:30ビルドの**後**に提出されたため、翌営業日(平日)ビルド待ちなだけと判明(1980.jsonのmtimeは18:30:28、動画提出は23:03と時系列で整合)

## 再現性確認
- `build_data._merge_member_videos()`を関数単体で、本番データのコピー(/tmp配下)に対して直接実行し、6769の動画が正しくマージされることを再確認(コードは正常動作)
- git log確認: `_merge_member_videos`関連コードは2026-05-28導入・2026-06-09に社名解決処理を追加して以降、無変更(直近の回帰なし)

## 副次確認(本調査と無関係な既存の差分)
調査中に`git status`で`dashboard/master.json`・`portfolio.json`・`manual_links.json`に差分があることに気づいたが、内容確認の結果、これは7740タムロン(2026-07-06 13:52提出)の通常運用による自動更新(社名自動補完・ポートフォリオ追加・手動リンク登録)であり、本調査の作業とは無関係(調査は読み取りと関数単体実行のみで、いずれのファイルにも書き込みを行っていない)。

## 制約の遵守
- port 8081系コード(app.py/index.html/build_data.py)は一切変更していない
- build_data.py本番実行は行わず、`_merge_member_videos()`の関数単体テストのみ(/tmpコピーのデータに対して実行、本番ファイルへの書き込みなし)
- 機密情報は含まれていない

## 参考(設計上の制約・今後の検討材料)
メンバー動画は「翌営業日18:30の日次バッチ」でのみ反映される設計のため、土日提出は月曜18:30まで、平日でも18:30以降の提出は翌営業日18:30まで反映されない(最大で約1〜2.5日のタイムラグ)。即時反映が必要な場合は「POST /api/member-video成功時に該当コードのみ即座に部分ビルドする」等の設計変更が考えられるが、今回のタスク範囲外(原因特定のみ)のため実装はしていない。

## commit
- obsidian-vault(セッション引き継ぎ追記のみ、コード修正なし): `9754012`
